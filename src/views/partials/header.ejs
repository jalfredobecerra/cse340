<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | CSE 340 Service Network
    </title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>
    <a class="skip-link" href="#main-content">Skip to main content</a>

    <header class="site-header">
        <div class="container site-header__bar">
            <a class="brand" href="/">
                <span class="brand__mark" aria-hidden="true">SN</span>
                <span>Service Network</span>
            </a>

            <button type="button" class="nav-toggle" id="navToggle" aria-expanded="false" aria-controls="mainNav">
                <span aria-hidden="true">&#9776;</span> Menu
            </button>

            <nav class="main-nav" id="mainNav" aria-label="Primary">
                <ul>
                    <li><a href="/" <% if (title==='Home' ) { %>aria-current="page"<% } %>>Home</a></li>
                    <li><a href="/organizations" <% if (title==='Our Partner Organizations' ) { %>aria-current="page"<%
                                } %>>Organizations</a></li>
                    <li><a href="/projects" <% if (title==='Service Projects' ) { %>aria-current="page"<% } %>
                                >Projects</a></li>
                    <li><a href="/categories" <% if (title==='Categories' ) { %>aria-current="page"<% } %>
                                >Categories</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main id="main-content">
        <div class="container">